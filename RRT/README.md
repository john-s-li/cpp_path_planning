To run the RRT planner, make sure to have [matplotlib-cpp](https://github.com/lava/matplotlib-cpp) installed in your root Python version (no conda).

```
$ mkdir build && cd build
$ cmake ..
$ make
```

To run, `./rrt`.